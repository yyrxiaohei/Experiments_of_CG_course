# 项目架构
项目采用 Python标准源码布局（src源码结构），将源码统一放在`src/`目录下，便于包管理、导入控制和项目维护。其中，单次实验均以 `Workn` 为单元，打包为单独的项目代码。

项目目录结构如下：
```
CG_Lab/
├── pyproject.toml
├── src/
│   └── Work1/
│   │   └── ...
│   └── Work2/
│       └── ...
```

环境管理上，基于 uv 管理虚拟环境与依赖。（该部分内容不在仓库存储范围内）